I am building a modular HTML5 Canvas 2-Player local multiplayer arcade game collection called "2 Player Challenge: Tournament Edition". I have a custom engine (see the index.html from March 2026) and I need you to write new mini-games for it. Here are the strict rules and the API of my engine:

The Engine Structure: Every game must be a Javascript class that extends GameBase. Do NOT output any HTML or CSS. Output **only** the Javascript code for the games. At the very bottom of each game file you must register it by calling: GameManager.registerGame(new YourGameClass());
The GameBase Class Properties: By extending GameBase, your class inherits: • this.name and this.description (set via super("Name", "Short description / goal."); in constructor) • this.scoreP1 and this.scoreP2 (numbers — increment them to track score) • this.width and this.height (canvas = 800×600) • Methods you MUST override: init(w, h), update(dt), render(ctx)
Visual Theme – VERY STRICT: Use **only** the global Theme object for colors (no literals like "#ff0000", "red", etc. except for very rare special effects): Theme.bg → dark background #1a1921 Theme.fg → white / neutral lines & text Theme.p1 → hot pink-red #FF2A54 (Player 1) Theme.p2 → mint green #00E59B (Player 2) Theme.accent → electric yellow #FFE600 (highlights, items, power-ups)
Input System: Use the global Input object (already instantiated). Player 1: WASD + Space → 'KeyW','KeyA','KeyS','KeyD','Space' Player 2: Arrow keys + Enter → 'ArrowUp','ArrowLeft','ArrowDown','ArrowRight','Enter'
Single-player (vs CPU) support – REQUIRED: In update(dt), **always** check the global boolean GameManager.isSinglePlayer. • If true → write simple, fun, not-too-perfect CPU logic for Player 2 • If false → read normal Player 2 keyboard input
Win Conditions: When win condition is reached (score, time, lives, etc.), call: GameManager.gameOver(winner) → winner = 1 (P1 victory) → winner = 2 (P2 / CPU victory) → winner = 0 (draw – rare)
Tournament compatibility: The engine now runs fixed **7-round tournaments**. Each game should feel fair & exciting in best-of-many format. First to win a round gets 1 tournament point (draws award 0). Example minimal skeleton: class ExampleGame extends GameBase {     constructor() {         super("Example Game", "First to 5 points wins.");     }     init(w, h) {         super.init(w, h);         // reset only if fresh game (tournament re-uses instance sometimes)         if (this.scoreP1 === undefined) {             this.scoreP1 = 0;             this.scoreP2 = 0;         }         // init entities, physics, timers...     }     update(dt) {         // ──────────────── Player 1 ────────────────         if (Input.isDown('KeyW')) { /* up */ }         // ──────────────── Player 2 or CPU ────────────────         if (GameManager.isSinglePlayer) {             // simple CPU logic here         } else {             if (Input.isDown('ArrowUp')) { /* up */ }         }         // game logic ...         // example win check         if (this.scoreP1 >= 5) GameManager.gameOver(1);         if (this.scoreP2 >= 5) GameManager.gameOver(2);     }     render(ctx) {         ctx.fillStyle = Theme.p1;         // draw player 1         ctx.fillStyle = Theme.p2;         // draw player 2     } } GameManager.registerGame(new ExampleGame()); Based on these exact rules and the current engine (March 2026 version with 7-round tournament and CPU labeling), i need 5 more games(return files name.js and code) only Hoops: A 2D physics basketball game; jump and throw to get the ball in the basket.
   Bowling: Stop a swinging meter to determine the angle and power of your bowling ball.
   Volley-Bomb: Volleyball, but the ball is a bomb with a ticking timer. Don't let it explode on your side!
   Mini-Golf: Top-down physics putt-putt; fewest strokes to the hole wins.
   Mini-Golf: Top-down physics putt-putt; turns, first to the hole wins.
      Keep code clean, readable, and well-commented.